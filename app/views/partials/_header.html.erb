<nav class="navbar navbar-expand-lg "style="background-color:#719BBB">
  <div class="container-fluid">
    <%= link_to root_path do %>
        <h1> Chq.To </h1>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <% if Current.user %>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <%= link_to "New link", links_type_path, :class=>"nav-link" %>
                </li>
                <li class="nav-item">
                    <%= button_to "Borrar cuenta",user_path(Current.user.id), method: :delete,  :class=>"nav-link" %>
                </li>
            </ul>
        <% end %>
    </div>
        <ul class="nav justify-content-end">
            <% unless Current.user %>
                <li class="nav-item">
                    <%= link_to login_path do %>
                        <button class="btn btn-light"> Iniciar sesión </button>
                    <% end %>
                </li>
            <% else %>
                <li class="nav-item">
                    <span class="nav-link disabled">
                        <%= Current.user.username %>
                    </span>
                </li>
                <li class="nav-item">
                    <%= link_to logout_path do %>
                        <button class="btn btn-light"> Cerrar sesión </button>
                    <% end %>
                </li>
            <% end %>
        </ul>
  </div>
</nav>
<div>
  <h1> Ingresa la contraseña para acceder al recurso </h1>
</div>
<div>
  <%= form_with(url: slugPass_path(@link.slug), data: { turbo: false }) do |form| %>

    <div class="row align-items-center">
        <div class="col-auto">
        <%= form.label :password, :class=>"col-form-label" %>
        </div>
        <div class="col-auto">
        <%= form.password_field :password, :class=>"form-control"%>
        </div>
    </div>

    <div>
        <%= submit_tag 'Ingresar'  %>
    </div>
  <% end %>
</div>

  